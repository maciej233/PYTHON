#configure snmp contact
---
  - name: Configure SNMP Contact
    hosts: all
    gather_facts: false
    connection: local        

    tasks:
      - name: configure snmp contact
        community.network.ce_snmp_contact:          
          contact: maciej_test
          state: present
                    
      - name: show output
        debug:
          var: output
